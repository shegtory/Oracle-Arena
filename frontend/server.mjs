import{createServer}from'node:http';import{readFile}from'node:fs/promises';import{extname,resolve}from'node:path';import receipt from'./api/trade-receipt.mjs';import history from'./api/trade-history.mjs';const port=Number(process.env.PORT||4173),types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.svg':'image/svg+xml','.json':'application/json'};createServer(async(req,res)=>{if(req.url?.startsWith('/api/trade-receipt'))return receipt(req,res);if(req.url?.startsWith('/api/trade-history'))return history(req,res);try{const url=req.url==='/'?'/index.html':req.url?.split('?')[0]||'/index.html',path=resolve('dist',`.${url}`),body=await readFile(path);res.setHeader('Content-Type',types[extname(path)]||'application/octet-stream');res.end(body)}catch{res.statusCode=404;res.end('Not found')}}).listen(port,()=>console.log(`Oracle Arena production server: http://127.0.0.1:${port}`));
